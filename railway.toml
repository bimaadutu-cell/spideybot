[build]
builder = "RAILPACK"

[deploy]
preDeployCommand = "npm db:push"
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
